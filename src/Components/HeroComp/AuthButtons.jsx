import React from "react";
import { Link } from "react-router-dom";
import { ShoppingCart,  } from "react-feather";

export const AuthButtons = (currentUser,location) => {
  // const { userName, userEmail, photo, uID, isEmailVerified } = user;

  return (

  );
};
